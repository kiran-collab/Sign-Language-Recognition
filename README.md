# Sign-Language-Recognition

To do's

Collect training data: Label sequences with their sign meanings.

Train the LSTM: Use the keypoints over time and labels (words).

Use argmax or decoder: Get the word prediction and map it to text.

Postprocess: Optionally run NLP correction for grammar.
